default_config:

stream:

homeassistant:
  internal_url: !env_var HA_INTERNAL_URL
  external_url: !env_var HA_EXTERNAL_URL

http:
  use_x_forwarded_for: true
  trusted_proxies:
    - 127.0.0.1
    - 172.16.0.0/12
    - 10.0.0.0/8
    - 192.168.0.0/16

mqtt:
  broker: !secret mqtt_host
  username: !secret mqtt_user
  password: !secret mqtt_password
  discovery: true
  cover:
    - name: "Cửa cuốn kho"
      unique_id: shed_garage_door
      device_class: garage
      state_topic: "shed/state/door"
      command_topic: "shed/cmd/door"
      payload_open: "OPEN"
      payload_close: "CLOSE"
      payload_stop: "STOP"
      state_open: "open"
      state_closed: "closed"
      optimistic: false

camera:
  - platform: generic
    name: "Shed AI Overlay"
    still_image_url: "http://127.0.0.1:5000/api/imou_2k/latest.jpg"
    stream_source: "http://127.0.0.1:5000/api/imou_2k_overlay/flv"
    verify_ssl: false

shell_command:
  frigate_fetch_annotated_snapshot: >-
    mkdir -p "/config/www/frigate_snapshots/{{ camera }}" &&
    (curl -fsSL
      -H "Authorization: Bearer {{ token }}"
      "{{ ha_base_url }}/api/frigate/notifications/{{ event_id }}/snapshot.jpg"
      -o "/config/www/frigate_snapshots/{{ camera }}/{{ filename }}"
    || curl -fsSL
      "{{ frigate_base_url }}/api/events/{{ event_id }}/snapshot.jpg?bbox=1&timestamp=1"
      -o "/config/www/frigate_snapshots/{{ camera }}/{{ filename }}")

lovelace:
  mode: yaml
